<Application x:Class="NMEA2000Analyzer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NMEA2000Analyzer"
             Startup="Application_Startup">
    <Application.Resources>
        <SolidColorBrush x:Key="MenuBackgroundBrush" Color="White" />
        <SolidColorBrush x:Key="MenuBorderBrush" Color="#D7D7D7" />
        <SolidColorBrush x:Key="MenuHoverBrush" Color="#EAF2FF" />
        <SolidColorBrush x:Key="MenuPressedBrush" Color="#D9E8FF" />
        <SolidColorBrush x:Key="MenuTextBrush" Color="#202020" />
        <SolidColorBrush x:Key="MenuDisabledTextBrush" Color="#8A8A8A" />

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource MenuBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MenuBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="{StaticResource MenuBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}" />
        </Style>

        <ControlTemplate x:Key="TopLevelMenuItemTemplate" TargetType="MenuItem">
            <Border x:Name="Root"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="Root" Property="Background" Value="{StaticResource MenuHoverBrush}" />
                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource MenuBorderBrush}" />
                </Trigger>
                <Trigger Property="IsSubmenuOpen" Value="True">
                    <Setter TargetName="Root" Property="Background" Value="{StaticResource MenuPressedBrush}" />
                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource MenuBorderBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource MenuDisabledTextBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="SubmenuItemTemplate" TargetType="MenuItem">
            <Border x:Name="Root"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    Padding="0"
                    SnapsToDevicePixels="True">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="18" />
                    </Grid.ColumnDefinitions>

                    <ContentPresenter Grid.Column="0"
                                      Margin="10,4,10,4"
                                      ContentSource="Header"
                                      RecognizesAccessKey="True"
                                      VerticalAlignment="Center" />

                    <Grid Grid.Column="1"
                          Margin="0,0,8,0"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center">
                        <TextBlock x:Name="CheckGlyph"
                                   Text="✓"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Visibility="Collapsed" />
                        <TextBlock x:Name="SubmenuGlyph"
                                   Text="›"
                                   FontSize="14"
                                   Visibility="Collapsed" />
                    </Grid>

                    <Popup x:Name="PART_Popup"
                           Placement="Right"
                           AllowsTransparency="True"
                           Focusable="False"
                           IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                           PopupAnimation="Fade">
                        <Border Background="{StaticResource MenuBackgroundBrush}"
                                BorderBrush="{StaticResource MenuBorderBrush}"
                                BorderThickness="1"
                                Padding="4"
                                SnapsToDevicePixels="True">
                            <ScrollViewer CanContentScroll="True">
                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                            </ScrollViewer>
                        </Border>
                    </Popup>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="Root" Property="Background" Value="{StaticResource MenuHoverBrush}" />
                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource MenuBorderBrush}" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="Role" Value="SubmenuHeader">
                    <Setter TargetName="SubmenuGlyph" Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource MenuDisabledTextBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="{StaticResource MenuBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MenuBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Template" Value="{StaticResource SubmenuItemTemplate}" />
            <Style.Triggers>
                <Trigger Property="Role" Value="TopLevelHeader">
                    <Setter Property="Template" Value="{StaticResource TopLevelMenuItemTemplate}" />
                </Trigger>
                <Trigger Property="Role" Value="TopLevelItem">
                    <Setter Property="Template" Value="{StaticResource TopLevelMenuItemTemplate}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
